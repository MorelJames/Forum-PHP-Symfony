{% extends "base.html.twig"%}

{% block body%}
   
   <h1>{{post.getTitle()}}</h1>
   <p>{{post.getContent()}}</p>

   {{form(formComment)}}

   <h2>Comments</h2>

   {% for comment in comments %}
      <p>{{comment.getContent()}}</p>
   {% endfor %}


{% endblock %}