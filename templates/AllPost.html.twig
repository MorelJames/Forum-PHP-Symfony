{% extends "base.html.twig"%}

{% block body%}
   
    {% for post in allPost %}
        <div class= "post">
            <a href="{{ path('post', {'postId': post.getId()}) }}">{{post.getTitle()}}</a>
            <p>{{post.getContent()}}</p>
        </div>
    {% endfor %} 

{% endblock %}